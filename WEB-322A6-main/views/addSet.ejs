<!DOCTYPE html>
<html lang="en" data-theme="cupcake">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Lego Set - Lego Collection</title>
  <link href="/css/main.css" rel="stylesheet">
</head>
<body class="bg-base-100 text-base-content">

  <!-- Updated Navbar -->
  <%- include('./partials/navbar', { page: '/lego/addSet' }) %>

  <!-- Hero Section -->
  <div class="hero bg-base-200 rounded-lg p-6 mb-4">
    <div class="hero-content text-center">
      <div class="max-w-md">
        <h1 class="text-5xl font-bold">Add a New Lego Set</h1>
        <p class="py-4">Please fill out the form below to add a new Lego set to our collection.</p>
      </div>
    </div>
  </div>

  <!-- Add Set Form -->
  <div class="container mx-auto p-4 max-w-lg bg-base-200 rounded-lg shadow-lg">
    <form action="/lego/addSet" method="POST"  enctype="application/x-www-form-urlencoded" class="space-y-4">
      
      <!-- Set Number -->
      <div class="form-control">
        <label class="label font-semibold">Set Number</label>
        <input type="text" name="set_num" placeholder="Enter the set's number" class="input input-bordered w-full" required>
      </div>
      
      <!-- Name -->
      <div class="form-control">
        <label class="label font-semibold">Name</label>
        <input type="text" name="name" placeholder="Enter the name of the set" class="input input-bordered w-full" required>
      </div>
      
      <!-- Year -->
      <div class="form-control">
        <label class="label font-semibold">Release Year</label>
        <input type="number" name="year" placeholder="Enter the year it was released" class="input input-bordered w-full" required>
      </div>
      
      <!-- Number of Parts -->
      <div class="form-control">
        <label class="label font-semibold">Number of Parts</label>
        <input type="number" name="num_parts" placeholder="Enter the total number of parts" class="input input-bordered w-full" required>
      </div>
      
      <!-- Image URL -->
      <div class="form-control">
        <label class="label font-semibold">Image URL</label>
        <input type="url" name="img_url" placeholder="Enter the URL of the set's image" class="input input-bordered w-full" required>
      </div>
      
      <!-- Theme Selection -->
      <div class="form-control">
        <label class="label font-semibold">Theme</label>
        <select name="theme_id" class="select select-bordered w-full" required>
          <option disabled selected>Select a theme</option>
          <% sets.forEach(theme => { %>
            <option class ="content-center text-gray-950" value="<%= theme.theme_id %>">
              <%= theme.name %>
            </option>
          <% }) %>
        </select>
      </div>

      <!-- Submit Button -->
      <div class="form-control mt-4 text-center">
        <button type="submit" class="btn btn-primary w-full">Add Set</button>
      </div>
      
    </form>
  </div>
  
</body>
</html>
